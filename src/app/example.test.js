// TODO: Delete this file if not needed
// This is a simple test file to demonstrate the testing setup.
import { describe, it, expect } from 'vitest'

describe('Example Test', () => {
  it('should add numbers correctly', () => {
    expect(1 + 1).toBe(2)
  })
})